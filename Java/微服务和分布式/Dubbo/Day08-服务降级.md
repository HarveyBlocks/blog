# 服务降级

一个机器上有广告服务, 日志服务, 支付服务

有时候, 机器太忙了, 

我们就需要把不重要的广告服务和日志服务停掉

把资源留给重要的支付服务

## 方式

### `"force:return null"`

表示消费方对该服务的方法调用都**直接返回null值**

不发起远程调用

用来屏蔽不重要服务不可用时对调用方的影响

### `"fail:return null"`

表示消费方对服务的方法调用在失败后, 再返回null, 

不抛异常

用来容忍不重要服务不稳定时对调用方的影响

