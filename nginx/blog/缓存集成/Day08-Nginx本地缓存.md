## Nginx缓存

在Nginx缓存

docker处理操作的时候, IO读取, 影响响应速度, 加入缓存的相关只是

响应速度要比响应速度要快

有了缓存之后, 就不用发送缓存到客户端

不存在, 只要响应到客户端

重新发送请求到客户端

查询到数据之后就把数据

下一次再访问的时候, 只要数据存在, 就可以直接向

Nginx

数据不一致

重复存放, 占用资源, 增加

缓存出现了问题



## 流程

1.  使用URL/状态码+业务+等等组成Key
2.  Key-MD5>加密
3.  放在缓存目录
4.  用加密Key的一部分作为文件存放等等
5.  分散存放在磁盘上

