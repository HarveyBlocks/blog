# 轮询

## DNS轮询

## DNS

域名系统/服务协议(DNS)是分布式网络目录服务

主要用于域名与IP地址的相互转换

一个域名可以带有多个IP地址

### 轮询

本地会缓存DNS的解析结果, 所以一台客户端里的缓存还在, 就还会去找原来的那个IP, 

DNS不会对服务器宕机做出及时回应, 即使从DNS中删除IP, 各大厂商为了节省访问时间, 不会实时更新更新DNS

DNS不会区分服务器的差距

