# 字符集

MySQL 5 允许对字符集进行多种操作

- 使用多种字符集来存储字符串
- 使用多种校对规则来比较字符串
- 在同一台服务器、同一个数据库或甚至在同一个表中使用不同字符集或校对规则来混合字符串
- 允许定义任何级别的字符集和校对规则

## 校对规则

许多的 MySQL 服务器默认采用 latin1 编码

```php
$db->query("SET NAMES utf8");
```

