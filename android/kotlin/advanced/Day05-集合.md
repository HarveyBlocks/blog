# 集合

仅述和Java不同的部分

## 构造

`mutableSetOf<String>()` 构造可写的集合

底层用Linked, 保证写入顺序和读出顺序一致

### 空`==`

`emptyListOf<String>()`

## range 和 Progression

## Iterable 和 Sequence

Iterable 每次操作后计算, Sequence 依据情况延迟一些操作的计算

