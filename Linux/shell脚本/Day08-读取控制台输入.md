# 控制台输入

参数不能解决所有问题

可能运行到一半, 需要依据情况让用户输入些啥, 我们提示些啥等等

例如删除时的`yes or no`

```shell
read [选项] 变量
```

参数

-   `-p` 提示信息
-   `-t` 秒, 等待时间, 缺省表永远

```shell
#!/usr/bin/env sh
read -p'输入:' a
echo "$a""?"
```

按回车,不输入不会等你, 直接往下执行

不会读入回车

