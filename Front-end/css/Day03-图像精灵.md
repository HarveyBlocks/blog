# 图像精灵

切割一个图片, 使其显示图像的一部分

在请求的过程中, 一副图片被切割的所有的部分, 只会请求一次(也就是请求一次这个完整的图像)

减少请求次数, 降低服务器压力, 同时不会产生多次请求之间造成的**网络传输延迟**

`background: url(img_navsprites.gif) -91px 0;`

-   图像URL
-   X 轴偏移, 为负数表示从图像的右端往左偏移
-   Y 轴偏移, 为负数同理

